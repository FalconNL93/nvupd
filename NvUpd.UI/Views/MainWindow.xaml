<Window x:Class="NvUpd.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Text="GPU Information:" />
            <Separator />
            <TextBlock Text="{Binding ViewModel.GpuInformation.Name}" />
            <TextBlock Text="{Binding ViewModel.GpuInformation.NiceDriverVersion}" />
            <Separator />
            <TextBlock Text="{Binding ViewModel.UpdateStatus}" />
            <TextBlock Text="{Binding ViewModel.NvidiaUpdate.LatestVersion}" />
            <Button
                Visibility="{Binding ViewModel.NvidiaUpdate.UpdateAvailable,  
                Converter={StaticResource BooleanToVisibilityConverter}}"
                Height="50"
                Content="Update" />
        </StackPanel>
    </Grid>
</Window>